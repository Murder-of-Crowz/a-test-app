import { Text, View } from "react-native";

export default function FlashcardScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Flashcard page — coming soon</Text>
    </View>
  );
}